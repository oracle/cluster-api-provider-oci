---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: ocimachinepools.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: OCIMachinePool
    listKind: OCIMachinePoolList
    plural: ocimachinepools
    singular: ocimachinepool
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OCIMachinePoolSpec defines the desired state of OCIMachinePool
            properties:
              instanceConfiguration:
                description: InstanceConfiguration defines the configuration of the
                  instance pool instances.
                properties:
                  agentConfig:
                    description: AgentConfig defines the options for the Oracle Cloud
                      Agent software running on the instance.
                    properties:
                      areAllPluginsDisabled:
                        description: |-
                          AreAllPluginsDisabled defines whether Oracle Cloud Agent can run all the available plugins.
                          This includes the management and monitoring plugins.
                          To get a list of available plugins, use the
                          ListInstanceagentAvailablePlugins
                          operation in the Oracle Cloud Agent API. For more information about the available plugins, see
                          Managing Plugins with Oracle Cloud Agent (https://docs.cloud.oracle.com/iaas/Content/Compute/Tasks/manage-plugins.htm).
                        type: boolean
                      isManagementDisabled:
                        description: |-
                          IsManagementDisabled defines whether Oracle Cloud Agent can run all the available management plugins.
                          Default value is false (management plugins are enabled).
                          These are the management plugins: OS Management Service Agent and Compute Instance
                          Run Command.
                          The management plugins are controlled by this parameter and by the per-plugin
                          configuration in the `pluginsConfig` object.
                          - If `isManagementDisabled` is true, all of the management plugins are disabled, regardless of
                          the per-plugin configuration.
                          - If `isManagementDisabled` is false, all of the management plugins are enabled. You
                          can optionally disable individual management plugins by providing a value in the `pluginsConfig`
                          object.
                        type: boolean
                      isMonitoringDisabled:
                        description: |-
                          IsMonitoringDisabled defines whether Oracle Cloud Agent can gather performance metrics and monitor the instance using the
                          monitoring plugins. Default value is false (monitoring plugins are enabled).
                          These are the monitoring plugins: Compute Instance Monitoring
                          and Custom Logs Monitoring.
                          The monitoring plugins are controlled by this parameter and by the per-plugin
                          configuration in the `pluginsConfig` object.
                          - If `isMonitoringDisabled` is true, all of the monitoring plugins are disabled, regardless of
                          the per-plugin configuration.
                          - If `isMonitoringDisabled` is false, all of the monitoring plugins are enabled. You
                          can optionally disable individual monitoring plugins by providing a value in the `pluginsConfig`
                          object.
                        type: boolean
                      pluginsConfigs:
                        description: PluginsConfig defines the configuration of plugins
                          associated with this instance.
                        items:
                          description: InstanceAgentPluginConfig defines the configuration
                            of plugins associated with this instance.
                          properties:
                            desiredState:
                              description: |-
                                DesiredState defines whether the plugin should be enabled or disabled.
                                To enable the monitoring and management plugins, the `isMonitoringDisabled` and
                                `isManagementDisabled` attributes must also be set to false.
                                The following values are supported:
                                * `ENABLED`
                                * `DISABLED`
                              type: string
                            name:
                              description: |-
                                Name defines the name of the plugin. To get a list of available plugins, use the
                                ListInstanceagentAvailablePlugins
                                operation in the Oracle Cloud Agent API. For more information about the available plugins, see
                                Managing Plugins with Oracle Cloud Agent (https://docs.cloud.oracle.com/iaas/Content/Compute/Tasks/manage-plugins.htm).
                              type: string
                          type: object
                        type: array
                    type: object
                  availabilityConfig:
                    description: |-
                      LaunchInstanceAvailabilityConfig defines the options for VM migration during infrastructure maintenance events and for defining
                      the availability of a VM instance after a maintenance event that impacts the underlying hardware.
                    properties:
                      isLiveMigrationPreferred:
                        description: |-
                          IsLiveMigrationPreferred defines whether to live migrate supported VM instances to a healthy physical VM host without
                          disrupting running instances during infrastructure maintenance events. If null, Oracle
                          chooses the best option for migrating the VM during infrastructure maintenance events.
                        type: boolean
                      recoveryAction:
                        description: |-
                          RecoveryAction defines the lifecycle state for an instance when it is recovered after infrastructure maintenance.
                          * `RESTORE_INSTANCE` - The instance is restored to the lifecycle state it was in before the maintenance event.
                          If the instance was running, it is automatically rebooted. This is the default action when a value is not set.
                          * `STOP_INSTANCE` - The instance is recovered in the stopped state.
                        type: string
                    type: object
                  capacityReservationId:
                    description: |-
                      CapacityReservationId defines the OCID of the compute capacity reservation this instance is launched under.
                      You can opt out of all default reservations by specifying an empty string as input for this field.
                      For more information, see Capacity Reservations (https://docs.cloud.oracle.com/iaas/Content/Compute/Tasks/reserve-capacity.htm#default).
                    type: string
                  clusterPlacementGroupId:
                    description: ClusterPlacementGroupId defines the OCID of the cluster
                      placement group of the instance.
                    type: string
                  dedicatedVmHostId:
                    description: DedicatedVmHostId defines the OCID of the dedicated
                      VM host.
                    type: string
                  extendedMetadata:
                    additionalProperties:
                      x-kubernetes-preserve-unknown-fields: true
                    description: |-
                      Additional metadata key/value pairs that provide non-string values.
                      Values support nested JSON objects and arrays and map to OCI InstanceConfigurationLaunchInstanceDetails.extendedMetadata.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  instanceConfigurationId:
                    type: string
                  instanceOptions:
                    description: InstanceOptions defines the instance options
                    properties:
                      areLegacyImdsEndpointsDisabled:
                        description: |-
                          Whether to disable the legacy (/v1) instance metadata service endpoints.
                          Customers who have migrated to /v2 should set this to true for added security.
                          Default is false.
                        type: boolean
                    type: object
                  instanceSourceViaImageConfig:
                    description: InstanceSourceViaImageConfig defines the options
                      for booting up instances via images
                    properties:
                      bootVolumeSizeInGBs:
                        description: |-
                          The size of boot volume. Please see https://docs.oracle.com/en-us/iaas/Content/Block/Tasks/extendingbootpartition.htm
                          to extend the boot volume size.
                        format: int64
                        type: integer
                      bootVolumeVpusPerGB:
                        description: |-
                          BootVolumeVpusPerGB defines the number of volume performance units (VPUs) that will be applied to this volume per GB,
                          representing the Block Volume service's elastic performance options.
                          See Block Volume Performance Levels (https://docs.cloud.oracle.com/iaas/Content/Block/Concepts/blockvolumeperformance.htm#perf_levels) for more information.
                          Allowed values:
                            * `10`: Represents Balanced option.
                            * `20`: Represents Higher Performance option.
                            * `30`-`120`: Represents the Ultra High Performance option.
                          For volumes with the auto-tuned performance feature enabled, this is set to the default (minimum) VPUs/GB.
                        format: int64
                        type: integer
                      imageId:
                        description: OCID of the image to be used to launch the instance.
                        type: string
                      kmsKeyId:
                        description: KmsKeyId defines the OCID of the Key Management
                          key to assign as the master encryption key for the boot
                          volume.
                        type: string
                    type: object
                  instanceVnicConfiguration:
                    description: MachinePoolNetworkDetails defines the configuration
                      options for the MachinePool primary VNIC.
                    properties:
                      assignIpv6Ip:
                        description: AssignIPv6 determines whether to assign an IPv6
                          address to the instance.
                        type: boolean
                      assignPrivateDnsRecord:
                        description: AssignPrivateDnsRecord defines whether the VNIC
                          should be assigned a DNS record.
                        type: boolean
                      assignPublicIp:
                        description: AssignPublicIp defines whether the instance should
                          have a public IP address.
                        type: boolean
                      displayName:
                        description: |-
                          DisplayName defines a user-friendly name. Does not have to be unique, and it's changeable.
                          Avoid entering confidential information.
                        type: string
                      hostnameLabel:
                        description: HostnameLabel defines the hostname for the VNIC's
                          primary private IP. Used for DNS.
                        type: string
                      ipv6AddressIpv6SubnetCidrPairDetails:
                        description: Ipv6AddressIpv6SubnetCidrPairDetails defines
                          IPv6 CIDR pair details for the primary VNIC.
                        items:
                          description: InstanceConfigurationIpv6AddressIpv6SubnetCidrPairDetails
                            defines an IPv6 address and subnet CIDR pair.
                          properties:
                            ipv6Address:
                              description: Ipv6Address is an available IPv6 address
                                of the subnet from a valid IPv6 prefix.
                              type: string
                            ipv6SubnetCidr:
                              description: Ipv6SubnetCidr is used to disambiguate
                                which subnet prefix should be used to create an IPv6
                                allocation.
                              type: string
                          type: object
                        type: array
                      nsgId:
                        description: |-
                          NSGId defines the ID of the NSG to use. This parameter takes priority over NsgNames.
                          Deprecated, please use MachinePoolNetworkDetails.NSGIds.
                        type: string
                      nsgIds:
                        description: NSGIds defines the list of NSG IDs to use. This
                          parameter takes priority over NsgNames.
                        items:
                          type: string
                        type: array
                      nsgNames:
                        description: NsgNames defines a list of the nsg names of the
                          network security groups (NSGs) to add the VNIC to.
                        items:
                          type: string
                        type: array
                      securityAttributes:
                        additionalProperties:
                          additionalProperties:
                            x-kubernetes-preserve-unknown-fields: true
                          type: object
                        description: SecurityAttributes are OCI security attributes
                          for the primary VNIC.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      skipSourceDestCheck:
                        description: SkipSourceDestCheck defines whether the source/destination
                          check is disabled on the VNIC.
                        type: boolean
                      subnetId:
                        description: SubnetId defines the ID of the subnet to use.
                          This parameter takes priority over SubnetName.
                        type: string
                      subnetName:
                        description: SubnetName defines the subnet name to use for
                          the VNIC.
                        type: string
                    type: object
                  ipxeScript:
                    description: IpxeScript is the custom iPXE script that will run
                      when the instance boots.
                    type: string
                  isPvEncryptionInTransitEnabled:
                    default: true
                    description: Is in transit encryption of volumes required.
                    type: boolean
                  launchMode:
                    description: |-
                      LaunchMode specifies the configuration mode for launching virtual machine instances.
                      Supported values are NATIVE, EMULATED, PARAVIRTUALIZED, CUSTOM, and ACCELERATEDPV.
                    type: string
                  launchOptions:
                    description: LaunchOptions defines the options for tuning the
                      compatibility and performance of VM shapes
                    properties:
                      bootVolumeType:
                        description: |-
                          BootVolumeType defines Emulation type for the boot volume.
                          * `ISCSI` - ISCSI attached block storage device.
                          * `SCSI` - Emulated SCSI disk.
                          * `IDE` - Emulated IDE disk.
                          * `VFIO` - Direct attached Virtual Function storage. This is the default option for local data
                          volumes on platform images.
                          * `PARAVIRTUALIZED` - Paravirtualized disk. This is the default for boot volumes and remote block
                          storage volumes on platform images.
                        type: string
                      firmware:
                        description: |-
                          Firmware defines the firmware used to boot VM. Select the option that matches your operating system.
                          * `BIOS` - Boot VM using BIOS style firmware. This is compatible with both 32 bit and 64 bit operating
                          systems that boot using MBR style bootloaders.
                          * `UEFI_64` - Boot VM using UEFI style firmware compatible with 64 bit operating systems. This is the
                          default for platform images.
                        type: string
                      isConsistentVolumeNamingEnabled:
                        description: IsConsistentVolumeNamingEnabled defines whether
                          to enable consistent volume naming feature. Defaults to
                          false.
                        type: boolean
                      networkType:
                        description: |-
                          NetworkType defines the emulation type for the physical network interface card (NIC).
                          * `E1000` - Emulated Gigabit ethernet controller. Compatible with Linux e1000 network driver.
                          * `VFIO` - Direct attached Virtual Function network controller. This is the networking type
                          when you launch an instance using hardware-assisted (SR-IOV) networking.
                          * `PARAVIRTUALIZED` - VM instances launch with paravirtualized devices using VirtIO drivers.
                        type: string
                      remoteDataVolumeType:
                        description: |-
                          RemoteDataVolumeType defines the emulation type for volume.
                          * `ISCSI` - ISCSI attached block storage device.
                          * `SCSI` - Emulated SCSI disk.
                          * `IDE` - Emulated IDE disk.
                          * `VFIO` - Direct attached Virtual Function storage. This is the default option for local data
                          volumes on platform images.
                          * `PARAVIRTUALIZED` - Paravirtualized disk. This is the default for boot volumes and remote block
                          storage volumes on platform images.
                        type: string
                    type: object
                  licensingConfigs:
                    description: LicensingConfigs defines licensing configurations
                      associated with target launch values.
                    items:
                      properties:
                        licenseType:
                          type: string
                        type:
                          type: string
                      type: object
                    type: array
                  metadata:
                    additionalProperties:
                      type: string
                    description: |-
                      Custom metadata key/value pairs that you provide, such as the SSH public key
                      required to connect to the instance.
                    type: object
                  platformConfig:
                    description: PlatformConfig defines the platform config parameters
                    properties:
                      amdMilanBmPlatformConfig:
                        properties:
                          areVirtualInstructionsEnabled:
                            type: boolean
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isAccessControlServiceEnabled:
                            type: boolean
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            type: string
                          percentageOfCoresEnabled:
                            type: integer
                        type: object
                      amdRomeBmGpuPlatformConfig:
                        properties:
                          areVirtualInstructionsEnabled:
                            type: boolean
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isAccessControlServiceEnabled:
                            type: boolean
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            type: string
                        type: object
                      amdRomeBmPlatformConfig:
                        properties:
                          areVirtualInstructionsEnabled:
                            type: boolean
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isAccessControlServiceEnabled:
                            type: boolean
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            type: string
                          percentageOfCoresEnabled:
                            type: integer
                        type: object
                      amdVmPlatformConfig:
                        properties:
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                        type: object
                      intelIcelakeBmPlatformConfig:
                        properties:
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            type: string
                          percentageOfCoresEnabled:
                            type: integer
                        type: object
                      intelSkylakeBmPlatformConfig:
                        properties:
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            type: string
                          percentageOfCoresEnabled:
                            type: integer
                        type: object
                      intelVmPlatformConfig:
                        properties:
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                        type: object
                      platformConfigType:
                        description: PlatformConfigTypeEnum defines the type of platform
                          configuration.
                        type: string
                    type: object
                  preemptibleInstanceConfig:
                    description: PreemptibleInstanceConfig Configuration options for
                      preemptible instances.
                    properties:
                      terminatePreemptionAction:
                        description: TerminatePreemptionAction terminates the preemptible
                          instance when it is interrupted for eviction.
                        properties:
                          preserveBootVolume:
                            description: PreserveBootVolume defines whether to preserve
                              the boot volume that was used to launch the preemptible
                              instance when the instance is terminated. Defaults to
                              false if not specified.
                            type: boolean
                        type: object
                    type: object
                  preferredMaintenanceAction:
                    description: PreferredMaintenanceAction defines the preferred
                      maintenance action for an instance.
                    type: string
                  securityAttributes:
                    additionalProperties:
                      additionalProperties:
                        x-kubernetes-preserve-unknown-fields: true
                      type: object
                    description: SecurityAttributes are OCI security attributes for
                      the launched instance.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  shape:
                    type: string
                  shapeConfig:
                    description: The shape configuration of the instance, applicable
                      for flex instances.
                    properties:
                      baselineOcpuUtilization:
                        description: |-
                          The baseline OCPU utilization for a subcore burstable VM instance. Leave this attribute blank for a
                          non-burstable instance, or explicitly specify non-burstable with `BASELINE_1_1`.
                          The following values are supported:
                          - `BASELINE_1_8` - baseline usage is 1/8 of an OCPU.
                          - `BASELINE_1_2` - baseline usage is 1/2 of an OCPU.
                          - `BASELINE_1_1` - baseline usage is an entire OCPU. This represents a non-burstable instance.
                        type: string
                      memoryInGBs:
                        description: The total amount of memory available to the instance,
                          in gigabytes.
                        type: string
                      nvmes:
                        description: Nvmes defines the number of NVMe drives to be
                          used for storage. A single drive has 6.8 TB available.
                        type: integer
                      ocpus:
                        description: The total number of OCPUs available to the instance.
                        type: string
                      vcpus:
                        description: The total number of VCPUs available to the instance.
                        type: integer
                    type: object
                type: object
              instanceDisplayNameFormatter:
                description: InstanceDisplayNameFormatter defines a formatter for
                  instance display names in the instance pool.
                type: string
              instanceHostnameFormatter:
                description: InstanceHostnameFormatter defines a formatter for instance
                  hostnames in the instance pool.
                type: string
              ocid:
                description: OCID is the OCID of the associated InstancePool
                type: string
              placementDetails:
                description: PlacementDetails defines the placement details of the
                  instance pool.
                items:
                  properties:
                    availabilityDomain:
                      description: The availability domain to place instances.
                      type: integer
                    primaryVnicSubnets:
                      description: PrimaryVnicSubnets defines primary VNIC subnet
                        placement details.
                      properties:
                        ipv6AddressIpv6SubnetCidrPairDetails:
                          description: Ipv6AddressIpv6SubnetCidrPairDetails defines
                            IPv6 CIDR pair details for placement.
                          items:
                            description: InstancePoolPlacementIpv6AddressIpv6SubnetCidrDetails
                              defines an IPv6 address and subnet CIDR pair for pool
                              placement.
                            properties:
                              ipv6SubnetCidr:
                                description: Ipv6SubnetCidr is used to disambiguate
                                  which subnet prefix should be used to create an
                                  IPv6 allocation.
                                type: string
                            type: object
                          type: array
                        isAssignIpv6Ip:
                          description: IsAssignIpv6Ip determines whether to assign
                            an IPv6 address at instance and VNIC creation.
                          type: boolean
                        subnetId:
                          description: SubnetId defines the subnet OCID for the primary
                            VNIC.
                          type: string
                      type: object
                  required:
                  - availabilityDomain
                  type: object
                type: array
              providerID:
                description: ProviderID is the OCID of the associated InstancePool
                  in a provider format
                type: string
              providerIDList:
                description: |-
                  ProviderIDList are the identification IDs of machine instances provided by the provider.
                  This field must match the provider IDs as seen on the node objects corresponding to a machine pool's machine instances.
                items:
                  type: string
                type: array
            type: object
          status:
            description: OCIMachinePoolStatus defines the observed state of OCIMachinePool
            properties:
              conditions:
                description: Conditions defines current service state of the OCIMachinePool.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                type: string
              failureReason:
                type: string
              infrastructureMachineKind:
                description: InfrastructureMachineKind is the kind of the infrastructure
                  resources behind MachinePool Machines.
                type: string
              ready:
                description: Ready is true when the provider resource is ready.
                type: boolean
              replicas:
                description: Replicas is the most recently observed number of replicas
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - name: v1beta2
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OCIMachinePoolSpec defines the desired state of OCIMachinePool
            properties:
              instanceConfiguration:
                description: InstanceConfiguration defines the configuration of the
                  instance pool instances.
                properties:
                  agentConfig:
                    description: AgentConfig defines the options for the Oracle Cloud
                      Agent software running on the instance.
                    properties:
                      areAllPluginsDisabled:
                        description: |-
                          AreAllPluginsDisabled defines whether Oracle Cloud Agent can run all the available plugins.
                          This includes the management and monitoring plugins.
                          To get a list of available plugins, use the
                          ListInstanceagentAvailablePlugins
                          operation in the Oracle Cloud Agent API. For more information about the available plugins, see
                          Managing Plugins with Oracle Cloud Agent (https://docs.cloud.oracle.com/iaas/Content/Compute/Tasks/manage-plugins.htm).
                        type: boolean
                      isManagementDisabled:
                        description: |-
                          IsManagementDisabled defines whether Oracle Cloud Agent can run all the available management plugins.
                          Default value is false (management plugins are enabled).
                          These are the management plugins: OS Management Service Agent and Compute Instance
                          Run Command.
                          The management plugins are controlled by this parameter and by the per-plugin
                          configuration in the `pluginsConfig` object.
                          - If `isManagementDisabled` is true, all of the management plugins are disabled, regardless of
                          the per-plugin configuration.
                          - If `isManagementDisabled` is false, all of the management plugins are enabled. You
                          can optionally disable individual management plugins by providing a value in the `pluginsConfig`
                          object.
                        type: boolean
                      isMonitoringDisabled:
                        description: |-
                          IsMonitoringDisabled defines whether Oracle Cloud Agent can gather performance metrics and monitor the instance using the
                          monitoring plugins. Default value is false (monitoring plugins are enabled).
                          These are the monitoring plugins: Compute Instance Monitoring
                          and Custom Logs Monitoring.
                          The monitoring plugins are controlled by this parameter and by the per-plugin
                          configuration in the `pluginsConfig` object.
                          - If `isMonitoringDisabled` is true, all of the monitoring plugins are disabled, regardless of
                          the per-plugin configuration.
                          - If `isMonitoringDisabled` is false, all of the monitoring plugins are enabled. You
                          can optionally disable individual monitoring plugins by providing a value in the `pluginsConfig`
                          object.
                        type: boolean
                      pluginsConfigs:
                        description: PluginsConfig defines the configuration of plugins
                          associated with this instance.
                        items:
                          description: InstanceAgentPluginConfig defines the configuration
                            of plugins associated with this instance.
                          properties:
                            desiredState:
                              description: |-
                                DesiredState defines whether the plugin should be enabled or disabled.
                                To enable the monitoring and management plugins, the `isMonitoringDisabled` and
                                `isManagementDisabled` attributes must also be set to false.
                                The following values are supported:
                                * `ENABLED`
                                * `DISABLED`
                              type: string
                            name:
                              description: |-
                                Name defines the name of the plugin. To get a list of available plugins, use the
                                ListInstanceagentAvailablePlugins
                                operation in the Oracle Cloud Agent API. For more information about the available plugins, see
                                Managing Plugins with Oracle Cloud Agent (https://docs.cloud.oracle.com/iaas/Content/Compute/Tasks/manage-plugins.htm).
                              type: string
                          type: object
                        type: array
                    type: object
                  availabilityConfig:
                    description: |-
                      LaunchInstanceAvailabilityConfig defines the options for VM migration during infrastructure maintenance events and for defining
                      the availability of a VM instance after a maintenance event that impacts the underlying hardware.
                    properties:
                      isLiveMigrationPreferred:
                        description: |-
                          IsLiveMigrationPreferred defines whether to live migrate supported VM instances to a healthy physical VM host without
                          disrupting running instances during infrastructure maintenance events. If null, Oracle
                          chooses the best option for migrating the VM during infrastructure maintenance events.
                        type: boolean
                      recoveryAction:
                        description: |-
                          RecoveryAction defines the lifecycle state for an instance when it is recovered after infrastructure maintenance.
                          * `RESTORE_INSTANCE` - The instance is restored to the lifecycle state it was in before the maintenance event.
                          If the instance was running, it is automatically rebooted. This is the default action when a value is not set.
                          * `STOP_INSTANCE` - The instance is recovered in the stopped state.
                        type: string
                    type: object
                  capacityReservationId:
                    description: |-
                      CapacityReservationId defines the OCID of the compute capacity reservation this instance is launched under.
                      You can opt out of all default reservations by specifying an empty string as input for this field.
                      For more information, see Capacity Reservations (https://docs.cloud.oracle.com/iaas/Content/Compute/Tasks/reserve-capacity.htm#default).
                    type: string
                  clusterPlacementGroupId:
                    description: ClusterPlacementGroupId defines the OCID of the cluster
                      placement group of the instance.
                    type: string
                  dedicatedVmHostId:
                    description: DedicatedVmHostId defines the OCID of the dedicated
                      VM host.
                    type: string
                  extendedMetadata:
                    additionalProperties:
                      x-kubernetes-preserve-unknown-fields: true
                    description: |-
                      Additional metadata key/value pairs that provide non-string values.
                      Values support nested JSON objects and arrays and map to OCI InstanceConfigurationLaunchInstanceDetails.extendedMetadata.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  instanceConfigurationId:
                    type: string
                  instanceOptions:
                    description: InstanceOptions defines the instance options
                    properties:
                      areLegacyImdsEndpointsDisabled:
                        description: |-
                          Whether to disable the legacy (/v1) instance metadata service endpoints.
                          Customers who have migrated to /v2 should set this to true for added security.
                          Default is false.
                        type: boolean
                    type: object
                  instanceSourceViaImageConfig:
                    description: InstanceSourceViaImageConfig defines the options
                      for booting up instances via images
                    properties:
                      bootVolumeSizeInGBs:
                        description: |-
                          The size of boot volume. Please see https://docs.oracle.com/en-us/iaas/Content/Block/Tasks/extendingbootpartition.htm
                          to extend the boot volume size.
                        format: int64
                        type: integer
                      bootVolumeVpusPerGB:
                        description: |-
                          BootVolumeVpusPerGB defines the number of volume performance units (VPUs) that will be applied to this volume per GB,
                          representing the Block Volume service's elastic performance options.
                          See Block Volume Performance Levels (https://docs.cloud.oracle.com/iaas/Content/Block/Concepts/blockvolumeperformance.htm#perf_levels) for more information.
                          Allowed values:
                            * `10`: Represents Balanced option.
                            * `20`: Represents Higher Performance option.
                            * `30`-`120`: Represents the Ultra High Performance option.
                          For volumes with the auto-tuned performance feature enabled, this is set to the default (minimum) VPUs/GB.
                        format: int64
                        type: integer
                      imageId:
                        description: OCID of the image to be used to launch the instance.
                        type: string
                      kmsKeyId:
                        description: KmsKeyId defines the OCID of the Key Management
                          key to assign as the master encryption key for the boot
                          volume.
                        type: string
                    type: object
                  instanceVnicConfiguration:
                    description: MachinePoolNetworkDetails defines the configuration
                      options for the MachinePool primary VNIC.
                    properties:
                      assignIpv6Ip:
                        description: AssignIPv6 determines whether to assign an IPv6
                          address to the instance.
                        type: boolean
                      assignPrivateDnsRecord:
                        description: AssignPrivateDnsRecord defines whether the VNIC
                          should be assigned a DNS record.
                        type: boolean
                      assignPublicIp:
                        description: AssignPublicIp defines whether the instance should
                          have a public IP address.
                        type: boolean
                      displayName:
                        description: |-
                          DisplayName defines a user-friendly name. Does not have to be unique, and it's changeable.
                          Avoid entering confidential information.
                        type: string
                      hostnameLabel:
                        description: HostnameLabel defines the hostname for the VNIC's
                          primary private IP. Used for DNS.
                        type: string
                      ipv6AddressIpv6SubnetCidrPairDetails:
                        description: Ipv6AddressIpv6SubnetCidrPairDetails defines
                          IPv6 CIDR pair details for the primary VNIC.
                        items:
                          description: InstanceConfigurationIpv6AddressIpv6SubnetCidrPairDetails
                            defines an IPv6 address and subnet CIDR pair.
                          properties:
                            ipv6Address:
                              description: Ipv6Address is an available IPv6 address
                                of the subnet from a valid IPv6 prefix.
                              type: string
                            ipv6SubnetCidr:
                              description: Ipv6SubnetCidr is used to disambiguate
                                which subnet prefix should be used to create an IPv6
                                allocation.
                              type: string
                          type: object
                        type: array
                      nsgId:
                        description: |-
                          NSGId defines the ID of the NSG to use. This parameter takes priority over NsgNames.
                          Deprecated, please use MachinePoolNetworkDetails.NSGIds.
                        type: string
                      nsgIds:
                        description: NSGIds defines the list of NSG IDs to use. This
                          parameter takes priority over NsgNames.
                        items:
                          type: string
                        type: array
                      nsgNames:
                        description: NsgNames defines a list of the nsg names of the
                          network security groups (NSGs) to add the VNIC to.
                        items:
                          type: string
                        type: array
                      securityAttributes:
                        additionalProperties:
                          additionalProperties:
                            x-kubernetes-preserve-unknown-fields: true
                          type: object
                        description: SecurityAttributes are OCI security attributes
                          for the primary VNIC.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      skipSourceDestCheck:
                        description: SkipSourceDestCheck defines whether the source/destination
                          check is disabled on the VNIC.
                        type: boolean
                      subnetId:
                        description: SubnetId defines the ID of the subnet to use.
                          This parameter takes priority over SubnetName.
                        type: string
                      subnetName:
                        description: SubnetName defines the subnet name to use for
                          the VNIC.
                        type: string
                    type: object
                  ipxeScript:
                    description: IpxeScript is the custom iPXE script that will run
                      when the instance boots.
                    type: string
                  isPvEncryptionInTransitEnabled:
                    description: Is in transit encryption of volumes required.
                    type: boolean
                  launchMode:
                    description: |-
                      LaunchMode specifies the configuration mode for launching virtual machine instances.
                      Supported values are NATIVE, EMULATED, PARAVIRTUALIZED, CUSTOM, and ACCELERATEDPV.
                    type: string
                  launchOptions:
                    description: LaunchOptions defines the options for tuning the
                      compatibility and performance of VM shapes
                    properties:
                      bootVolumeType:
                        description: |-
                          BootVolumeType defines Emulation type for the boot volume.
                          * `ISCSI` - ISCSI attached block storage device.
                          * `SCSI` - Emulated SCSI disk.
                          * `IDE` - Emulated IDE disk.
                          * `VFIO` - Direct attached Virtual Function storage. This is the default option for local data
                          volumes on platform images.
                          * `PARAVIRTUALIZED` - Paravirtualized disk. This is the default for boot volumes and remote block
                          storage volumes on platform images.
                        type: string
                      firmware:
                        description: |-
                          Firmware defines the firmware used to boot VM. Select the option that matches your operating system.
                          * `BIOS` - Boot VM using BIOS style firmware. This is compatible with both 32 bit and 64 bit operating
                          systems that boot using MBR style bootloaders.
                          * `UEFI_64` - Boot VM using UEFI style firmware compatible with 64 bit operating systems. This is the
                          default for platform images.
                        type: string
                      isConsistentVolumeNamingEnabled:
                        description: IsConsistentVolumeNamingEnabled defines whether
                          to enable consistent volume naming feature. Defaults to
                          false.
                        type: boolean
                      networkType:
                        description: |-
                          NetworkType defines the emulation type for the physical network interface card (NIC).
                          * `E1000` - Emulated Gigabit ethernet controller. Compatible with Linux e1000 network driver.
                          * `VFIO` - Direct attached Virtual Function network controller. This is the networking type
                          when you launch an instance using hardware-assisted (SR-IOV) networking.
                          * `PARAVIRTUALIZED` - VM instances launch with paravirtualized devices using VirtIO drivers.
                        type: string
                      remoteDataVolumeType:
                        description: |-
                          RemoteDataVolumeType defines the emulation type for volume.
                          * `ISCSI` - ISCSI attached block storage device.
                          * `SCSI` - Emulated SCSI disk.
                          * `IDE` - Emulated IDE disk.
                          * `VFIO` - Direct attached Virtual Function storage. This is the default option for local data
                          volumes on platform images.
                          * `PARAVIRTUALIZED` - Paravirtualized disk. This is the default for boot volumes and remote block
                          storage volumes on platform images.
                        type: string
                    type: object
                  licensingConfigs:
                    description: LicensingConfigs defines licensing configurations
                      associated with target launch values.
                    items:
                      description: LaunchInstanceLicensingConfig defines a launch
                        licensing configuration.
                      properties:
                        licenseType:
                          description: LicenseType defines the OS license type.
                          type: string
                        type:
                          description: Type identifies the licensing config type.
                          type: string
                      type: object
                    type: array
                  metadata:
                    additionalProperties:
                      type: string
                    description: |-
                      Custom metadata key/value pairs that you provide, such as the SSH public key
                      required to connect to the instance.
                    type: object
                  platformConfig:
                    description: PlatformConfig defines the platform config parameters
                    properties:
                      amdMilanBmPlatformConfig:
                        description: AmdMilanBmPlatformConfig describes AMD Milan
                          BM platform configuration.
                        properties:
                          areVirtualInstructionsEnabled:
                            type: boolean
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isAccessControlServiceEnabled:
                            type: boolean
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            description: AmdMilanBmPlatformConfigNumaNodesPerSocketEnum
                              defines AMD Milan NUMA node options.
                            type: string
                          percentageOfCoresEnabled:
                            type: integer
                        type: object
                      amdRomeBmGpuPlatformConfig:
                        description: AmdRomeBmGpuPlatformConfig describes AMD Rome
                          BM GPU platform configuration.
                        properties:
                          areVirtualInstructionsEnabled:
                            type: boolean
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isAccessControlServiceEnabled:
                            type: boolean
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            description: AmdRomeBmGpuPlatformConfigNumaNodesPerSocketEnum
                              defines AMD Rome GPU NUMA node options.
                            type: string
                        type: object
                      amdRomeBmPlatformConfig:
                        description: AmdRomeBmPlatformConfig describes AMD Rome BM
                          platform configuration.
                        properties:
                          areVirtualInstructionsEnabled:
                            type: boolean
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isAccessControlServiceEnabled:
                            type: boolean
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            description: AmdRomeBmPlatformConfigNumaNodesPerSocketEnum
                              defines AMD Rome NUMA node options.
                            type: string
                          percentageOfCoresEnabled:
                            type: integer
                        type: object
                      amdVmPlatformConfig:
                        description: AmdVmPlatformConfig describes AMD VM platform
                          configuration.
                        properties:
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                        type: object
                      intelIcelakeBmPlatformConfig:
                        description: IntelIcelakeBmPlatformConfig describes Intel
                          Icelake BM platform configuration.
                        properties:
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            description: IntelIcelakeBmPlatformConfigNumaNodesPerSocketEnum
                              defines Intel Icelake NUMA node options.
                            type: string
                          percentageOfCoresEnabled:
                            type: integer
                        type: object
                      intelSkylakeBmPlatformConfig:
                        description: IntelSkylakeBmPlatformConfig describes Intel
                          Skylake BM platform configuration.
                        properties:
                          configMap:
                            additionalProperties:
                              type: string
                            type: object
                          isInputOutputMemoryManagementUnitEnabled:
                            type: boolean
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                          numaNodesPerSocket:
                            description: IntelSkylakeBmPlatformConfigNumaNodesPerSocketEnum
                              defines Intel Skylake NUMA node options.
                            type: string
                          percentageOfCoresEnabled:
                            type: integer
                        type: object
                      intelVmPlatformConfig:
                        description: IntelVmPlatformConfig describes Intel VM platform
                          configuration.
                        properties:
                          isMeasuredBootEnabled:
                            type: boolean
                          isMemoryEncryptionEnabled:
                            type: boolean
                          isSecureBootEnabled:
                            type: boolean
                          isSymmetricMultiThreadingEnabled:
                            type: boolean
                          isTrustedPlatformModuleEnabled:
                            type: boolean
                        type: object
                      platformConfigType:
                        description: PlatformConfigTypeEnum defines the type of platform
                          configuration.
                        type: string
                    type: object
                  preemptibleInstanceConfig:
                    description: PreemptibleInstanceConfig Configuration options for
                      preemptible instances.
                    properties:
                      terminatePreemptionAction:
                        description: TerminatePreemptionAction terminates the preemptible
                          instance when it is interrupted for eviction.
                        properties:
                          preserveBootVolume:
                            description: PreserveBootVolume defines whether to preserve
                              the boot volume that was used to launch the preemptible
                              instance when the instance is terminated. Defaults to
                              false if not specified.
                            type: boolean
                        type: object
                    type: object
                  preferredMaintenanceAction:
                    description: PreferredMaintenanceAction defines the preferred
                      maintenance action for an instance.
                    type: string
                  securityAttributes:
                    additionalProperties:
                      additionalProperties:
                        x-kubernetes-preserve-unknown-fields: true
                      type: object
                    description: SecurityAttributes are OCI security attributes for
                      the launched instance.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  shape:
                    type: string
                  shapeConfig:
                    description: The shape configuration of the instance, applicable
                      for flex instances.
                    properties:
                      baselineOcpuUtilization:
                        description: |-
                          The baseline OCPU utilization for a subcore burstable VM instance. Leave this attribute blank for a
                          non-burstable instance, or explicitly specify non-burstable with `BASELINE_1_1`.
                          The following values are supported:
                          - `BASELINE_1_8` - baseline usage is 1/8 of an OCPU.
                          - `BASELINE_1_2` - baseline usage is 1/2 of an OCPU.
                          - `BASELINE_1_1` - baseline usage is an entire OCPU. This represents a non-burstable instance.
                        type: string
                      memoryInGBs:
                        description: The total amount of memory available to the instance,
                          in gigabytes.
                        type: string
                      nvmes:
                        description: Nvmes defines the number of NVMe drives to be
                          used for storage. A single drive has 6.8 TB available.
                        type: integer
                      ocpus:
                        description: The total number of OCPUs available to the instance.
                        type: string
                      vcpus:
                        description: The total number of VCPUs available to the instance.
                        type: integer
                    type: object
                type: object
              instanceDisplayNameFormatter:
                description: InstanceDisplayNameFormatter defines a formatter for
                  instance display names in the instance pool.
                type: string
              instanceHostnameFormatter:
                description: InstanceHostnameFormatter defines a formatter for instance
                  hostnames in the instance pool.
                type: string
              ocid:
                description: OCID is the OCID of the associated InstancePool
                type: string
              placementDetails:
                description: PlacementDetails defines the placement details of the
                  instance pool.
                items:
                  properties:
                    availabilityDomain:
                      description: The availability domain to place instances.
                      type: integer
                    primaryVnicSubnets:
                      description: PrimaryVnicSubnets defines primary VNIC subnet
                        placement details.
                      properties:
                        ipv6AddressIpv6SubnetCidrPairDetails:
                          description: Ipv6AddressIpv6SubnetCidrPairDetails defines
                            IPv6 CIDR pair details for placement.
                          items:
                            description: InstancePoolPlacementIpv6AddressIpv6SubnetCidrDetails
                              defines an IPv6 address and subnet CIDR pair for pool
                              placement.
                            properties:
                              ipv6SubnetCidr:
                                description: Ipv6SubnetCidr is used to disambiguate
                                  which subnet prefix should be used to create an
                                  IPv6 allocation.
                                type: string
                            type: object
                          type: array
                        isAssignIpv6Ip:
                          description: IsAssignIpv6Ip determines whether to assign
                            an IPv6 address at instance and VNIC creation.
                          type: boolean
                        subnetId:
                          description: SubnetId defines the subnet OCID for the primary
                            VNIC.
                          type: string
                      type: object
                  required:
                  - availabilityDomain
                  type: object
                type: array
              providerID:
                description: ProviderID is the OCID of the associated InstancePool
                  in a provider format
                type: string
              providerIDList:
                description: |-
                  ProviderIDList are the identification IDs of machine instances provided by the provider.
                  This field must match the provider IDs as seen on the node objects corresponding to a machine pool's machine instances.
                items:
                  type: string
                type: array
            type: object
          status:
            description: OCIMachinePoolStatus defines the observed state of OCIMachinePool
            properties:
              conditions:
                description: Conditions defines current service state of the OCIMachinePool.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                type: string
              failureReason:
                type: string
              infrastructureMachineKind:
                description: InfrastructureMachineKind is the kind of the infrastructure
                  resources behind MachinePool Machines.
                type: string
              ready:
                description: Ready is true when the provider resource is ready.
                type: boolean
              replicas:
                description: Replicas is the most recently observed number of replicas
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
