---
apiVersion: infrastructure.cluster.x-k8s.io/v1beta2
kind: OCIMachineTemplate
metadata:
  namespace: "${NAMESPACE}"
  name: "${CLUSTER_NAME}-md-0"
spec:
  template:
    spec:
      networkDetails:
        assignIpv6Ip: true